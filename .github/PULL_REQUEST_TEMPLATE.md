what changed.

why — the bug it fixes, or the thing it makes possible.

if it touches ranking (`search`, `enrich`, `chunker`), the `fyc eval` numbers
before and after: recall@1, recall@3, recall@10, mrr, and on which eval file.

`pytest` passes locally.
